#include <gtest/gtest.h>


extern "C" {
    #include "core/expression.h"
}


/*
TEST(Symbol, new) {
    Symbol* s = Symbol_new("a");
    ASSERT_TRUE(s != NULL);
    EXPECT_STREQ(s->name, "a");
    Symbol_free(s);
}
*/
